.venv/
__pycache__/
*.pyc
data/
*.npz
*.pgn
*.pgn.zst
.pytest_cache/
checkpoints/
.claude/settings.local.json
sahformer_engine.bat
selfplay*.html

# Kaggle upload artifacts (regenerate with scripts/kaggle_train.py + build)
kaggle_train_bundle/
kaggle_train_bundle.zip
data/shards_v3/
overnight.log
remote_bundle/
remote_bundle.tar.gz

# --- keep the repo pushable: weights, caches and third-party PDFs stay local ---
# (.gitignore has no trailing comments - every pattern gets its own line)
# fine-tuned clones, ~78 MB each, 1.9 GB total
clones/
# encoded position cache, 3.5 GB
cache/
# any checkpoint outside checkpoints/, e.g. base_v3_step98000.pt
*.pt
# third-party paper PDFs, 290 MB; the NOTES_*.md summaries stay tracked
lit/pdf/
kaggle_bundle/
*.zip
# run logs; the findings they produced live in results/clonefish/*.md and *.json
*.log
.claude/
# third-party Stockfish binary used by the evaluation harness (see results/clonefish/FINAL.md)
tools/
